<p class="well"><%= @entry.title %></p>
<div class = "hero-unit">
  <%= @entry.entry %>
</div>
<div class = "hero-unit">

<p>
  <b>Comments:</b>
  <% @journal_comments.each do |journal_comment|  %>
  <% @commenters.each do |commenter| %>
  <ul>
    <li>Comment: <%= journal_comment.body %> </li>
    <br/>
    <li>Commenter name: <%= commenter.name %> </li>
  </ul>
  <% end %>
  <% end %>
</p>


</div>

<%= link_to 'Add Comment', new_journal_comment_path %> |
<%= link_to 'Back', journal_path(@entry.journal_id) %>
